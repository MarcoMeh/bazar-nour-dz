# 📋 دليل صفحة تسجيل المحلات الجديدة

## ✅ ما تم إنجازه:

### 1. صفحة التسجيل (`src/pages/SellerRegister.tsx`)

#### الميزات:
- ✅ نموذج تسجيل كامل مع validation
- ✅ حقول:
  - اسم صاحب المحل
  - اسم المحل
  - رقم الهاتف (مع validation للأرقام الجزائرية)
  - البريد الإلكتروني
  - الولاية (dropdown بكل الولايات الـ58)
  - وصف المحل (اختياري)
- ✅ تصميم احترافي بألوان جذابة
- ✅ Icons واضحة لكل حقل
- ✅ رسائل تأكيد وأخطاء
- ✅ قسم فوائد انضمام المحل

#### Validation:
```javascript
- رقم الهاتف: /^(0)(5|6|7)[0-9]{8}$/
  (مثال: 0555123456)
  
- البريد الإلكتروني: standard email regex
  
- جميع الحقول مطلوبة ما عدا الوصف
```

#### كيفية الوصول:
```
الرابط: /seller-register
```

---

### 2. قاعدة البيانات (`db_store_registration.sql`)

#### الجدول الجديد: `store_registration_requests`

```sql
الأعمدة:
- id: UUID (primary key)
- owner_name: TEXT (اسم صاحب المحل)
- store_name: TEXT (اسم المحل)
- phone: TEXT (رقم الهاتف)
- email: TEXT (البريد الإلكتروني)
- wilaya: TEXT (الولاية)
- description: TEXT (وصف المحل - nullable)
- status: TEXT (pending/approved/rejected)
- admin_notes: TEXT (ملاحظات الأدمن)
- created_at: TIMESTAMPTZ
- updated_at: TIMESTAMPTZ
```

#### RLS Policies:
- ✅ أي شخص يمكنه إرسال طلب (INSERT)
- ✅ فقط الأدمن يمكنهم عرض الطلبات (SELECT)
- ✅ فقط الأدمن يمكنهم تحديث الطلبات (UPDATE)

#### Indexes للأداء:
- `idx_store_reg_status` على status
- `idx_store_reg_created` على created_at

---

### 3. التصنيفات المتعددة (`db_store_multiple_categories.sql`)

#### الجدول الجديد: `store_categories`

```sql
جدول ربط Many-to-Many بين stores و categories

الأعمدة:
- id: UUID
- store_id: UUID (FK -> stores)
- category_id: UUID (FK -> categories)
- created_at: TIMESTAMPTZ
- UNIQUE(store_id, category_id) -- لمنع التكرار
```

#### Migration تلقائي:
```sql
-- نقل البيانات الموجودة من stores.category_id
INSERT INTO store_categories (store_id, category_id)
SELECT id, category_id FROM stores
WHERE category_id IS NOT NULL
```

#### كيفية الاستخدام:

**إضافة تصنيفات لمحل:**
```sql
INSERT INTO store_categories (store_id, category_id)
VALUES 
  ('store-uuid-here', 'category-1-uuid'),
  ('store-uuid-here', 'category-2-uuid');
```

**عرض جميع تصنيفات محل:**
```sql
SELECT c.* 
FROM categories c
JOIN store_categories sc ON c.id = sc.category_id
WHERE sc.store_id = 'store-uuid-here';
```

**عرض المحلات حسب تصنيف:**
```sql
SELECT s.*
FROM stores s
JOIN store_categories sc ON s.id = sc.store_id
WHERE sc.category_id = 'category-uuid-here';
```

---

## 🚀 خطوات التطبيق:

### 1. تشغيل SQL في Supabase:

```bash
# 1. جدول طلبات التسجيل
نفذ محتوى: db_store_registration.sql

# 2. جدول التصنيفات المتعددة
نفذ محتوى: db_store_multiple_categories.sql
```

### 2. الصفحة جاهزة:
- الرابط: `http://localhost:5173/seller-register`
- لا يحتاج تسجيل دخول
- يرسل البيانات مباشرة لقاعدة البيانات

### 3. للأدمن - عرض الطلبات:
يمكنك إنشاء صفحة في لوحة الأدمن لعرض الطلبات:

```sql
-- عرض الطلبات المعلقة
SELECT * FROM store_registration_requests
WHERE status = 'pending'
ORDER BY created_at DESC;

-- الموافقة على طلب
UPDATE store_registration_requests
SET status = 'approved', admin_notes = 'تم الموافقة'
WHERE id = 'request-uuid';
```

---

## 📱 User Flow (رحلة المستخدم):

1. المستخدم يضغط "افتح متجرك مجاناً" في الصفحة الرئيسية
2. ينتقل إلى `/seller-register`
3. يملأ النموذج بمعلوماته
4. يضغط "إرسال الطلب"
5. تظهر رسالة نجاح "تم إرسال طلبك بنجاح!"
6. يُحفظ في قاعدة البيانات بحالة `pending`
7. الأدمن يراجع الطلب ويوافق/يرفض
8. يتم التواصل مع صاحب المحل

---

## 🎨 التصميم:

### الألوان:
- Primary: Green gradient (للزر والأيقونات)
- Background: Gradient من green-50 إلى blue-50
- Cards: أبيض مع shadow

### المكونات:
- Form fields بحجم كبير (h-12)
- Icons من Lucide React
- Validation messages واضحة
- Loading state مع spinner
- Success redirect بعد ثانيتين

### Responsive:
- Mobile-first design
- Grid columns تتكيف (md:grid-cols-3)
- Padding مناسب لجميع الشاشات

---

## 🔧 التحديثات المطلوبة في المستقبل:

### للأدمن:
1. **صفحة إدارة الطلبات** في لوحة الأدمن:
   - عرض جدول بكل الطلبات
   - فلاتر حسب الحالة (pending, approved, rejected)
   - أزرار موافقة/رفض
   - حقل ملاحظات

2. **Email Notifications** (اختياري):
   - إرسال email عند الموافقة
   - إرسال email عند الرفض

### للمحلات:
1. **Multi-Category Selector** في صفحة تعديل المحل:
   - Checkbox أو Multi-select
   - يسمح باختيار عدة تصنيفات
   - حفظ في `store_categories`

---

## ✨ الميزات الإضافية المضافة:

### في الصفحة الرئيسية:
- زر "افتح متجرك مجاناً" يوجه إلى `/seller-register`

### قائمة الولايات الكاملة:
58 ولاية جزائرية مدرجة في الـ dropdown

### Benefits Section:
- 🚀 ابدأ بسرعة
- 💰 بدون عمولات
- 📈 وصول أوسع

---

هل تريد:
1. إنشاء صفحة إدارة الطلبات للأدمن؟
2. تحديث صفحة إضافة/تعديل المحل لدعم التصنيفات المتعددة؟
3. المتابعة بميزة أخرى (Brands, Sale Pages)؟
